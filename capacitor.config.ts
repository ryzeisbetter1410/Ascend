import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.distractionfree.app',
  appName: 'Distraction Free App',
  webDir: 'out'
};

export default config;
